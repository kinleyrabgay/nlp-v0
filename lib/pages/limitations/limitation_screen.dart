import 'package:flutter/material.dart';

class LimitationScreen extends StatelessWidget {
  const LimitationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
